<%= grid_item_tag_for(record) do %>
    <%= render_record_thumb(record, :size => "100x80#") %>
    <%= grid_item_caption_tag do %>
      <%= render_record_title(record) %> 
    <% end %> 
<% end if record && record.promo_text.present? %>